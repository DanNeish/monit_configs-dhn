#Monitoring Postgres Service

check process postgresql with pidfile <%= node['monit_configs-dhn']['postgres']['pidfile'] %>
  start program = "/bin/systemctl start postgresql.service"
  stop program = "/bin/systemctl stop postgresql.service"
  if 15 restarts within 15 cycles then timeout
